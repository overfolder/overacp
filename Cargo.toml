[workspace]
resolver = "2"
members = [
    "loop",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
repository = "https://github.com/overfolder/overacp"

[profile.release]
lto = true
strip = true
codegen-units = 1
